# Database Configuration for Prisma Migrations
# These variables are used by: pnpm --filter @kudos/db exec prisma migrate deploy

# Production/Development Database
DATABASE_URL=postgresql://kudos:kudos@localhost:5432/kudos?schema=public

# Test Database (used during test runs)
TEST_DATABASE_URL=postgresql://test:test@localhost:5433/test?schema=public
